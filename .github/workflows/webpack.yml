name: NodeJS with Webpack

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: node-latest
    
    steps:
    - uses: actions/checkout@v3

    - name: Build
      run: |
        npm install
        npx build
        
